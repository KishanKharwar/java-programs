package com.ds.ashokit;

/**
 * k=1
 * least number of radio frequency required to that it can cover all the house
 * if arr represents location of the houses, find min number of radio used to broadcast the frequency
 *
 */
public class RadioFrequency {

  public static void main(String[] args) {
    int[] arr = {2,4,5,6,9,11,12};
    int k = 2;
    minRadio(arr,arr.length,k);
  }

  private static void minRadio(int[] arr, int n, int k) {

  }
}
